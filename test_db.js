import { createClient } from @supabase/supabase-js; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function run() { const {data} = await supabase.from(cards).select(due_date).limit(1); console.log(data); } run();
import fs from fs; import { createClient } from @supabase/supabase-js; const env = fs.readFileSync(.env, utf8); const url = env.match(/VITE_SUPABASE_URL=(.*)/)[1]; const key = env.match(/VITE_SUPABASE_ANON_KEY=(.*)/)[1]; const supabase = createClient(url, key); async function run() { const {data} = await supabase.from(cards).select(due_date).limit(5); console.log(data); } run();
